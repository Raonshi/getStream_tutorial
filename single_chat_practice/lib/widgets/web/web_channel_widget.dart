import 'package:flutter/material.dart';

class WebChannelWidget extends StatelessWidget {
  const WebChannelWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
      'Chatting Room',
      textAlign: TextAlign.center,
    );
  }
}
